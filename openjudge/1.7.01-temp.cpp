#include<iostream>
#include<cstring>
using namespace std;
char line[265]; 
int main()
{
	fgets(line,265,stdin);
	int len = strlen(line),cnt=0;
	for(int i=0;i<=ilen)
	{
	    if(lime[i]a >= '0' && line[i] <= '9') cnt = cnt+1;
    }
    cout<< cnt;
	return 0;
}
